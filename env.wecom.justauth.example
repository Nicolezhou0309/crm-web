# 企业微信登录配置 - JustAuth最佳实践版本
# 注意：所有敏感配置现在在后端处理，前端只需要公开的配置

# 企业微信公开配置（前端使用）
VITE_WECOM_CORP_ID=ww68a125fce698cb59
VITE_WECOM_AGENT_ID=1000002
VITE_WECOM_REDIRECT_URI=https://yourdomain.com/auth/wecom/callback

# ⚠️ 以下配置已移至后端，不再在前端使用
# VITE_WECOM_SECRET=your_secret_here  # 已移除，后端处理

# 后端API配置
VITE_API_BASE_URL=https://yourdomain.com/api

# 说明：
# 1. VITE_WECOM_CORP_ID: 企业ID，公开信息，前端可以使用
# 2. VITE_WECOM_AGENT_ID: 应用ID，公开信息，前端可以使用  
# 3. VITE_WECOM_REDIRECT_URI: 回调地址，公开信息，前端可以使用
# 4. VITE_WECOM_SECRET: 应用密钥，敏感信息，仅后端使用
# 5. VITE_API_BASE_URL: 后端API基础地址，用于前端调用后端接口
